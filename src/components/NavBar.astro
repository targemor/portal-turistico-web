---
import ReactNavBar from "./react/NavBar";

interface Props {
  forceBackground?: boolean;
}

const { forceBackground = false } = Astro.props;
---

<ReactNavBar forceBackground={forceBackground} client:load />
