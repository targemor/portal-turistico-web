---
import type { Imperdible } from "../types";
import ImperdiblesReact from "./react/ImperdiblesSection";

interface Props {
  imperdibles: Imperdible[];
}

const { imperdibles } = Astro.props;
---

<ImperdiblesReact imperdibles={imperdibles} client:only="react" />
