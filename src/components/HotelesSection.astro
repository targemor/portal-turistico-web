---
/**
 * HotelesSection – Hoteles desde JSON, estilo fiel a test.jsx
 */
import ReactCard from "./react/Card";
import Button from "./ui/Button.astro";
import ArrowRightIcon from "../assets/icons/ArrowRightIcon.astro";

import type { Hotel } from "../types";

interface Props {
  hoteles: Hotel[];
}

const { hoteles } = Astro.props;
---

<section id="hoteles" class="py-20 bg-white border-y border-slate-100">
  <div class="container mx-auto px-6">
    <!-- Header -->
    <div class="flex flex-col md:flex-row justify-between items-end mb-12 gap-4">
      <div>
        <p class="text-brand font-bold text-xs uppercase tracking-[0.3em] mb-2" data-i18n="hotelBadge">Alojamiento</p>
        <h2 class="text-3xl font-bold tracking-tight text-slate-900" data-i18n="hotelTitle">DONDE HOSPEDARSE</h2>
      </div>
      <Button href="/directorio/hoteles" variant="outline" class="text-xs font-bold gap-2">
        <span data-i18n="hotelCta">VER TODOS LOS HOTELES</span>
        <ArrowRightIcon class="w-3.5 h-3.5" />
      </Button>
    </div>

    <!-- Grid -->
    <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
      {hoteles.map((hotel) => (
        <ReactCard client:visible item={hotel} categoria="hoteles" />
      ))}
    </div>
  </div>
</section>
